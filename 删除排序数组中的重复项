int removeDuplicates(int* nums, int numsSize){
    if (numsSize==0)
    return 0;
    int flag=0;
    for(int i=0;i<numsSize-1;i++)
    {
        if(nums[i]!=nums[i+1])
        {
            flag++;
            nums[flag]=nums[i+1];
        }
    }
    return flag+1;
}
